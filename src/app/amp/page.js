export const config = {
    amp: true, // Enables standalone AMP mode
  };
  
  export default function AMPPage() {
    return (
      <div>
        <h1>Standalone AMP Page</h1>
        <p>This page is served exclusively in AMP format.</p>
      </div>
    );
  }
  